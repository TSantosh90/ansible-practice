- name: given number is less than 10 or not
  hosts: local
  connection: local
  vars:
   NUMBER_OF_SESSION: 7
  tasks:
  - name: is ansible is more than 5 sessions
    ansible.builtin.debug:
     msg: "yes,ansible is more than 5 sessions"
    when: NUMBER_OF_SESSIONS > 5