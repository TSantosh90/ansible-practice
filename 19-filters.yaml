- name: demo on filters
  hosts: local
  connection: local
  vars:
    topics: mysql,ansible,zip,nginx
  tasks:
  - name: greetings
    ansible.builtin.debug:
     msg: " Hi Santosh kumar, {{ GREETINGS | default('good morning') }}"

  - name: convert string to list
    ansible.builtin.debug:
     msg: "{{ topics | split(',') }}"